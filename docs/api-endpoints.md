# API Endpoints

Initial planned endpoints:

- POST /users/register
- POST /users/login
- GET /book-clubs
- GET /book-clubs/{id}
- POST /book-clubs/{id}/join
- POST /votes
- GET /votes/results
- POST /comments
- PATCH /reading-progress

This work is linked to Jira item SCRUM-26.
